.container.app-container
  .input-group.input-group-lg
    .input-group-btn
      a(ng-href="analysis/new").new-corpus.btn.btn-primary
        | Create Analysis
    input.form-control(placeholder="Search by corpus name, analysis type..." ng-model="searchText")
  br
  div(ng-show="!analyses.length")
    alert(type='warning')
      | You haven't created any analyses.
  .list-group
    a.list-group-item(ng-repeat="analysis in analyses",ng-href="analysis/{{analysis._id}}")
      h4.list-group-item-heading
        strong {{ analysis.analysis_name }}
      p.list-group-item-text
        div(ng-repeat="corpora_id in analysis.corpora_ids")  Corpora: {{ findCorpus(corpora_id).title }}

        span Analysis: {{analysis.analysis}}
        br
        span Cleanups:
        span(ng-repeat="cleanup in analysis.cleanup_ids") {{cleanup}}
        br
        span Tokenizer: {{analysis.tokenizer}}
        br
        span Time Created: {{analysis.timestamp}}

